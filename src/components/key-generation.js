/** @jsx Preact.h */
import Preact from 'preact';
import styles from './key-generation.scss';

class KeyGeneration extends Preact.Component {
  render() {
    return <div className={styles.container} />;
  }
}

export default KeyGeneration;
